# TaxFlow 100K 基准

运行日期：2026-08-11  
运行命令：`python -m flagship_lab.cli benchmark --db work/benchmark-100k-v1.db --rows 100000`

## 结果

| 指标 | 实测值 |
|---|---:|
| 合成交易 | 100,000 条 |
| 规则发现 | 5,889 条 |
| 数据生成 | 0.6212 秒 |
| SQLite 批量入库 | 1.3447 秒 |
| 四项规则扫描与结果落库 | 0.5159 秒 |
| 端到端吞吐 | 40,293.85 条/秒 |

本结果只代表当前本地运行环境、SQLite WAL 模式、固定随机种子和四项演示规则，不应外推为生产性能。基准数据库不会被命令自动覆盖，避免误把历史结果当作新测试。

## 质量状态

- 六项自动化测试通过。
- Python `compileall` 通过。
- 已验证税号缺失、税额重算、重复发票、极端金额四项规则。
- 已验证中文检索误命中缺陷：由单字分词修正为二元词组，并增加查询覆盖率门槛。

